/** Type privé posé dans `DataTransfer` pour ne jamais accepter un autre glissé
 * (texte, image, fichier) comme un lot de produits du catalogue. */
export const PRODUCT_BATCH_DRAG_TYPE =
	'application/x-pocketapp-product-selection'
